services:
  ollama:
    image: ollama/ollama:latest
    container_name: derm-advisor-ollama
    ports:
      - "11434:11434"
    restart: unless-stopped
    volumes:
      - ollama-data:/root/.ollama

volumes:
  ollama-data:
